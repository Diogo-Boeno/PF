--!strict
-- The dumb one. Notices late, presses sloppily, and has no `parry` key at all — so it
-- can only ever raise a block, never turn a swing around.

return {
	awareness = 0.45,
	answer = { block = 1 },
	jitter = 0.1,
	aggression = 0.8,
	attackCooldown = 1.6,
}
